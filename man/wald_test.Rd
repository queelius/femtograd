% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypothesis.R
\name{wald_test}
\alias{wald_test}
\alias{wald_test.femtofit}
\alias{wald_test.default}
\title{Wald Test for Model Parameters}
\usage{
wald_test(object, ...)

\method{wald_test}{femtofit}(object, parm = NULL, null_value = 0, ...)

\method{wald_test}{default}(object, se, null_value = 0, ...)
}
\arguments{
\item{object}{A \code{femtofit} object, or for the generic: the parameter estimate.}

\item{...}{Additional arguments.}

\item{parm}{Parameter name(s) or indices to test. If NULL, tests all parameters.}

\item{null_value}{The null hypothesis value(s). Default is 0.}

\item{se}{Standard error (for default method)}
}
\value{
For single parameter tests, a \code{wald_test} object (also inherits from
\code{hypothesis_test}) containing:
\describe{
\item{stat}{The Wald chi-squared statistic}
\item{p.value}{P-value from chi-squared(1) distribution}
\item{dof}{Degrees of freedom (1 for single parameter)}
\item{z}{The z-score}
\item{estimate}{Parameter estimate}
\item{se}{Standard error}
\item{null_value}{The null hypothesis value}
}

For multiple parameters, returns a list of wald_test objects.
}
\description{
Performs Wald tests for individual parameters or linear combinations
of parameters in a fitted model.
}
\details{
The Wald test statistic for a single parameter is:
\deqn{W = \left(\frac{\hat{\theta} - \theta_0}{SE(\hat{\theta})}\right)^2}

which follows a chi-squared distribution with 1 degree of freedom under H0.
}
\section{Methods (by class)}{
\itemize{
\item \code{wald_test(femtofit)}: Wald test for femtofit objects

\item \code{wald_test(default)}: Wald test from raw estimate and SE

}}
\examples{
\dontrun{
set.seed(42)
x <- rnorm(100, mean = 5, sd = 2)

result <- fit(
  function(mu, log_sigma) loglik_normal(mu, exp(log_sigma), x),
  params = c(mu = 0, log_sigma = 0)
)

# Test if mu = 0
test <- wald_test(result, "mu")
pval(test)

# Test if mu = 5
wald_test(result, "mu", null_value = 5)

# Test all parameters
wald_test(result)
}

}
