% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootstrap_fit}
\alias{bootstrap_fit}
\title{Bootstrap standard errors and confidence intervals}
\usage{
bootstrap_fit(loglik_maker, data, params, n_boot = 500, progress = TRUE, ...)
}
\arguments{
\item{loglik_maker}{A function that takes data and returns a log-likelihood
function suitable for \code{fit()}. See examples.}

\item{data}{The data (vector, matrix, or data frame).}

\item{params}{Initial parameter values for \code{fit()}.}

\item{n_boot}{Number of bootstrap replications (default 500).}

\item{progress}{Logical; show progress messages (default TRUE).}

\item{...}{Additional arguments passed to \code{fit()}.}
}
\value{
A \code{bootstrap_result} object containing:
\describe{
\item{estimates}{Matrix of bootstrap estimates (n_boot x n_params)}
\item{original}{Original parameter estimates}
\item{se}{Bootstrap standard errors}
\item{bias}{Estimated bias}
}
}
\description{
Performs bootstrap resampling to estimate the sampling distribution
of parameter estimates.
}
\details{
The \code{loglik_maker} function should accept data and return a log-likelihood
function that can be passed to \code{fit()}. This design allows bootstrap
to work with any model specification.
}
\examples{
\dontrun{
set.seed(42)
x <- rnorm(50, mean = 5, sd = 2)

# Define a function that creates the loglik function from data
make_loglik <- function(data) {
  function(mu, log_sigma) {
    loglik_normal(mu, exp(log_sigma), data)
  }
}

# Bootstrap
boot <- bootstrap_fit(
  make_loglik,
  data = x,
  params = c(mu = 0, log_sigma = 0),
  n_boot = 200
)

# Results
boot$se          # Bootstrap SEs
confint(boot)    # Bootstrap CIs

# Compare with Wald SEs
result <- fit(make_loglik(x), params = c(mu = 0, log_sigma = 0))
se(result)       # Wald SEs
}

}
