% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distributions.R
\name{loglik_logistic}
\alias{loglik_logistic}
\title{Logistic regression log-likelihood (binary)}
\usage{
loglik_logistic(beta, X, y)
}
\arguments{
\item{beta}{Coefficient vector (list of value objects)}

\item{X}{Design matrix (n x p numeric matrix)}

\item{y}{Binary response vector (0 or 1)}
}
\value{
A value object representing the log-likelihood
}
\description{
Computes the log-likelihood for binary logistic regression.
L(β|X,y) = Σ\link{yᵢ*log(pᵢ) + (1-yᵢ)*log(1-pᵢ)}
where pᵢ = sigmoid(Xᵢ·β)
}
\details{
Uses the numerically stable form:
log(p) = -log(1 + exp(-η)) and log(1-p) = -log(1 + exp(η))
where η = Xβ
}
